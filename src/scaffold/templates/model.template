import {
  Table,
  Column,
  Model,
  Default,
  DataType,
  ForeignKey,
  BelongsTo,
  AllowNull
} from "sequelize-typescript";

import { {{#each enums}}{{name}},{{/each}} } from "@src/modules/{{pluralize name}}/{{pluralize name}}.enum.ts";

@Table
export class {{capitalize name}} extends Model<{{capitalize name}}> {
{{#each fields}}
    @Column({
        type: {{seqType}},
        {{#each options}}
        {{@key}}:{{this}},
        {{/each}}
    })
    {{name}}: {{tsType}};

{{/each}}
}